'use strict';

describe('eBanking.accounts module', function() {

  beforeEach(module('eBanking.accounts'));

  describe('accounts controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('accountsController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});