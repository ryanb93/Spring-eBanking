'use strict';

describe('eBankingApp.customerDetailsController module', function() {

  beforeEach(module('eBankingApp.customerDetailsController'));

  describe('customerDetailsController controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var customerDetailsViewCtrl = $controller('CustomerDetailsViewController');
      expect(customerDetailsViewCtrl).toBeDefined();
    }));

  });
});